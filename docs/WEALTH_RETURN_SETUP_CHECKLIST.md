# 财富产品收益追踪系统 - 部署检查清单\n\n## 📋 部署前检查\n\n### 环境准备\n- [ ] Node.js 版本 ≥ 16.x\n- [ ] PostgreSQL 版本 ≥ 12.x\n- [ ] 数据库备份已创建\n- [ ] 开发环境和生产环境变量已配置\n\n### 权限检查\n- [ ] finapp_user 数据库用户权限充足\n- [ ] 后端服务有数据库访问权限\n- [ ] API 服务可以正常启动\n\n---\n\n## 🗄️ 第一步：数据库迁移\n\n### 1.1 备份数据库\n```bash\n# 执行命令\npg_dump -h localhost -U finapp_user -d finapp_test > \\\n  /Users/caojun/code/FinApp/backups/wealth_backup_$(date +%Y%m%d_%H%M%S).sql\n```\n- [ ] 备份命令执行成功\n- [ ] 备份文件大小正常（> 100MB）\n- [ ] 备份文件位置：`/Users/caojun/code/FinApp/backups/`\n- [ ] 备份文件名格式：`wealth_backup_20251108_HHMMSS.sql`\n\n### 1.2 执行迁移脚本\n```bash\n# 执行命令\npsql -h localhost -U finapp_user -d finapp_test -f \\\n  /Users/caojun/code/FinApp/backend/migrations/011_wealth_product_returns/up.sql\n```\n- [ ] 脚本执行无错误\n- [ ] 收到完成提示信息\n- [ ] 没有权限错误\n- [ ] 没有约束冲突\n\n### 1.3 验证迁移成功\n\n#### 验证表创建\n```sql\n-- 执行查询验证\nSELECT table_name FROM information_schema.tables \nWHERE table_schema='finapp' AND table_name LIKE 'wealth%';\n```\n- [ ] `wealth_product_transactions` 表已创建\n- [ ] `wealth_product_nav_history` 表已创建\n- [ ] 表的所有者是 `finapp_user`\n\n#### 验证字段扩展\n```sql\nSELECT column_name FROM information_schema.columns \nWHERE table_name='wealth_product_details' AND table_schema='finapp' \nORDER BY ordinal_position;\n```\n- [ ] `product_subtype` 列存在\n- [ ] `expected_annual_return` 列存在\n- [ ] `ytd_return` 列存在\n- [ ] `total_dividends_received` 列存在\n- [ ] `dividend_frequency` 列存在\n- [ ] `nav_currency` 列存在\n- [ ] `settlement_method` 列存在\n\n#### 验证索引创建\n```sql\nSELECT indexname FROM pg_indexes \nWHERE schemaname='finapp' AND tablename LIKE 'wealth%';\n```\n- [ ] `idx_wealth_transactions_asset` 索引存在\n- [ ] `idx_wealth_transactions_date` 索引存在\n- [ ] `idx_wealth_transactions_type` 索引存在\n- [ ] `idx_nav_history_asset` 索引存在\n- [ ] `idx_nav_history_date` 索引存在\n- [ ] `idx_nav_history_unique` 唯一索引存在\n\n#### 验证视图创建\n```sql\nSELECT viewname FROM pg_views \nWHERE schemaname='finapp' AND viewname LIKE 'vw_%';\n```\n- [ ] `vw_wealth_product_returns` 视图存在\n- [ ] `vw_deviation_analysis` 视图存在\n- [ ] 视图可正常查询（SELECT * LIMIT 1）\n\n#### 验证触发器创建\n```sql\nSELECT trigger_name FROM information_schema.triggers \nWHERE trigger_schema='finapp' AND trigger_name LIKE '%wealth%';\n```\n- [ ] `wealth_transactions_update_timestamp` 触发器存在\n\n### 1.4 初始化数据\n```sql\n-- 为现有产品设置产品子类型\nUPDATE finapp.wealth_product_details\nSET product_subtype = 'DIVIDEND'\nWHERE return_type IS NOT NULL\n  AND product_subtype IS NULL;\n```\n- [ ] 现有理财产品已分类\n- [ ] 检查分类是否正确\n\n---\n\n## 🔧 第二步：后端服务集成\n\n### 2.1 文件检查\n- [ ] `WealthProductReturnService.ts` 文件已复制到 `backend/src/services/`\n- [ ] 文件内容完整无缺\n- [ ] TypeScript 语法检查通过\n\n### 2.2 导入和注册\n```typescript\n// backend/src/services/index.ts\nexport { WealthProductReturnService, wealthProductReturnService } from './WealthProductReturnService';\n```\n- [ ] 导出语句已添加\n- [ ] 导入路径正确\n- [ ] 没有循环依赖\n\n### 2.3 编译检查\n```bash\n# 执行编译\ncd /Users/caojun/code/FinApp\nnpm run build\n```\n- [ ] 编译无错误\n- [ ] 编译无警告（或警告可接受）\n- [ ] 编译产物输出到 `dist/` 目录\n\n### 2.4 服务功能验证\n```bash\n# 启动开发服务\nnpm run dev\n\n# 在另一个终端测试\nnode -e \"console.log('测试完成')\"\n```\n- [ ] 后端服务启动无错误\n- [ ] 没有缺失依赖\n- [ ] 数据库连接正常\n- [ ] 服务日志正常\n\n---\n\n## 🌐 第三步：API 接口实现（待做）\n\n### 3.1 创建路由文件\n```bash\n# 创建文件\ntouch /Users/caojun/code/FinApp/backend/src/routes/wealth.ts\n```\n- [ ] 文件已创建\n- [ ] 路由导入正确\n- [ ] 路由注册到主应用\n\n### 3.2 实现 API 端点\n- [ ] `GET /api/wealth/:assetId/return` - 获取收益\n- [ ] `POST /api/wealth/:assetId/transaction` - 记录交易\n- [ ] `GET /api/wealth/:assetId/deviation-analysis` - 偏差分析\n- [ ] `GET /api/wealth/:assetId/trend` - 趋势数据\n\n### 3.3 API 测试\n```bash\n# 测试获取收益\ncurl -X GET http://localhost:3001/api/wealth/test-id/return \\\n  -H \"Authorization: Bearer test-token\"\n```\n- [ ] API 响应 200 状态码\n- [ ] 响应 JSON 格式正确\n- [ ] 包含所有必需字段\n- [ ] 数据精度正确\n\n---\n\n## 💻 第四步：前端组件实现（待做）\n\n### 4.1 创建组件\n```bash\n# 创建文件\nmkdir -p /Users/caojun/code/FinApp/frontend/src/components/wealth\ntouch /Users/caojun/code/FinApp/frontend/src/components/wealth/WealthProductDashboard.tsx\n```\n- [ ] 组件文件已创建\n- [ ] 组件导入正确\n- [ ] 组件注册到页面\n\n### 4.2 实现组件功能\n- [ ] 预期 vs 实际收益对比\n- [ ] 偏差趋势图表\n- [ ] 交易记录表格\n- [ ] 风险告警提示\n- [ ] 分红/净值类型切换\n\n### 4.3 样式和交互\n- [ ] 响应式布局\n- [ ] 数据加载状态\n- [ ] 错误处理\n- [ ] 用户交互反馈\n\n### 4.4 前端构建和测试\n```bash\n# 构建前端\ncd /Users/caojun/code/FinApp/frontend\nnpm run build\n```\n- [ ] 前端构建成功\n- [ ] 没有编译错误\n- [ ] 不存在 TypeScript 类型错误\n- [ ] 页面在浏览器中正常显示\n\n---\n\n## 📊 第五步：监控和告警系统（待做）\n\n### 5.1 创建监控任务\n```bash\n# 创建文件\ntouch /Users/caojun/code/FinApp/backend/src/jobs/wealthMonitoring.ts\n```\n- [ ] 监控文件已创建\n- [ ] 定时任务配置正确\n- [ ] 告警规则已定义\n\n### 5.2 配置监控规则\n- [ ] 偏差率告警阈值配置\n- [ ] 分红延迟告警规则\n- [ ] 净值下跌告警规则\n- [ ] 费用异常告警规则\n\n### 5.3 集成通知系统\n- [ ] 邮件通知已集成\n- [ ] 短信通知已集成\n- [ ] 应用内通知已实现\n- [ ] 通知模板已创建\n\n### 5.4 监控测试\n```bash\n# 启动监控任务\nnode dist/jobs/wealthMonitoring.js\n```\n- [ ] 监控任务启动无错误\n- [ ] 首次运行成功执行\n- [ ] 告警规则触发正确\n- [ ] 通知正确发送\n\n---\n\n## 🧪 第六步：测试验证\n\n### 6.1 单元测试\n```bash\n# 运行单元测试\ncd /Users/caojun/code/FinApp/backend\nnpm run test -- WealthProductReturnService\n```\n- [ ] 所有测试用例通过\n- [ ] 测试覆盖率 ≥ 80%\n- [ ] 边界情况已测试\n- [ ] 错误处理已测试\n\n### 6.2 集成测试\n```bash\n# 运行集成测试\nnpm run test:integration -- wealth\n```\n- [ ] 数据库操作正确\n- [ ] API 调用成功\n- [ ] 事务处理正确\n- [ ] 数据一致性检查通过\n\n### 6.3 功能测试\n\n#### 测试场景 1：分红型产品\n```\n1. 创建分红型产品\n2. 记录购买交易\n3. 记录分红交易\n4. 验证收益计算\n5. 验证偏差分析\n```\n- [ ] 分红产品可正常创建\n- [ ] 交易记录可正常保存\n- [ ] 收益计算正确\n- [ ] 偏差分析准确\n\n#### 测试场景 2：净值型产品\n```\n1. 创建净值型产品\n2. 记录申购交易\n3. 更新净值历史\n4. 验证收益计算\n5. 验证偏差分析\n```\n- [ ] 净值产品可正常创建\n- [ ] 净值更新可保存\n- [ ] 收益计算正确\n- [ ] 偏差分析准确\n\n#### 测试场景 3：告警功能\n```\n1. 创建产品\n2. 生成告警条件\n3. 验证告警触发\n4. 验证通知发送\n```\n- [ ] 告警规则正确触发\n- [ ] 通知正确发送\n- [ ] 告警日志记录完整\n\n### 6.4 性能测试\n```bash\n# 运行性能测试\nnpm run test:performance -- wealth\n```\n- [ ] 单笔交易查询 < 100ms\n- [ ] 偏差计算 < 500ms\n- [ ] 大批量数据导入 < 5s\n- [ ] 报告生成 < 2s\n\n---\n\n## 🚀 第七步：部署验证\n\n### 7.1 开发环境验证\n- [ ] 开发环境所有测试通过\n- [ ] 没有控制台错误\n- [ ] 没有内存泄漏\n- [ ] 没有性能瓶颈\n\n### 7.2 测试环境部署\n```bash\n# 部署到测试环境\nbash /Users/caojun/code/FinApp/deploy-staging.sh\n```\n- [ ] 部署脚本执行成功\n- [ ] 测试环境数据库迁移成功\n- [ ] 测试环境服务启动成功\n- [ ] 测试用例运行通过\n\n### 7.3 灰度发布\n- [ ] 灰度发布配置完成\n- [ ] 10% 用户可访问新功能\n- [ ] 监控告警系统正常\n- [ ] 无用户投诉\n- [ ] 灰度期持续至少 1 周\n\n### 7.4 全量发布\n- [ ] 灰度期观察无异常\n- [ ] 生产环境数据库备份\n- [ ] 生产环境数据库迁移\n- [ ] 生产环境服务部署\n- [ ] 生产环境验证\n\n---\n\n## 📈 第八步：上线后监控\n\n### 8.1 实时监控\n- [ ] 应用错误率 < 0.1%\n- [ ] API 响应时间 < 500ms\n- [ ] 数据库连接池健康\n- [ ] 缓存命中率 > 80%\n\n### 8.2 数据质量\n- [ ] 收益计算准确率 > 99%\n- [ ] 数据一致性检查通过\n- [ ] 没有数据重复或缺失\n- [ ] 历史数据正确迁移\n\n### 8.3 用户反馈\n- [ ] 收集用户反馈\n- [ ] 处理用户投诉\n- [ ] 优化用户体验\n- [ ] 发布改进版本\n\n### 8.4 持续优化\n- [ ] 分析用户行为数据\n- [ ] 优化性能瓶颈\n- [ ] 完善告警规则\n- [ ] 增强功能特性\n\n---\n\n## 📝 检查清单总结\n\n### 关键里程碑\n- [ ] **第1-2周**：数据库迁移 + 后端服务集成\n- [ ] **第2-3周**：API 接口实现 + 前端组件开发\n- [ ] **第3-4周**：监控系统实现 + 完整测试\n- [ ] **第4-5周**：灰度发布 + 上线验证\n- [ ] **第5周+**：全量发布 + 持续监控\n\n### 成功标准\n- ✅ 所有检查项都已完成\n- ✅ 系统测试覆盖率 ≥ 80%\n- ✅ 没有关键 Bug\n- ✅ 性能指标达标\n- ✅ 用户反馈积极\n\n### 应急预案\n- 🔄 如遇严重问题，立即回滚到之前版本\n- 📞 联系技术支持团队\n- 💾 使用备份数据恢复\n\n---\n\n## ✅ 最终签核\n\n**部署开始日期**: _____________  \n**预计完成日期**: _____________  \n**实际完成日期**: _____________  \n\n**部署负责人**: _____________  \n**技术审核人**: _____________  \n**产品经理**: _____________  \n\n---\n\n**版本**: 1.0  \n**创建日期**: 2025-11-08  \n**适用范围**: 财富产品收益追踪系统全面部署\n